<h1> Showing all thoughts... </h1>

<table> 
	<head>
		<tr>
			<th>Title</th>
			<th>Description</th>
			<th colspan="3">Actions</th>
			<th>Likes</th>
		</tr>
	</thead>
	
	<tbody> <!-- This is where we put embedded ruby  -->
		<% @thoughts.each do |thought| %>
			<tr>
				<td><%= thought.title %></td>
				<td><%= thought.description %></td>
				<td><%= link_to 'Show', thought_path(thought) %></td>
				<td><%= link_to 'Edit', edit_thought_path(thought) %></td>
				<td><%= link_to 'Delete', thought_path(thought), method: :delete, data: {confirm: "Are you sure you want to delete?"} %></td>
			</tr>
		<% end %>
	</tbody>
</table>

<p>
	<%= link_to 'Create new thought', new_thought_path %>
</p>